# 電話番号の運用メモ（固定電話の復活手順）

## 現在の状態（2026-06-02 〜）

サイト表示の電話番号を **携帯 `080-3999-0379` に統一**しています。
固定電話 `0476-29-5498` は**削除せず、HTML コメントとして原文のまま各所に保存**してあります。
後日、固定電話を復活させたいときは、このメモの手順でコメントを解除するだけで元に戻せます。

| 区分 | 番号 | 国際表記（`tel:` / JSON-LD 用） |
|---|---|---|
| 携帯（現在表示中） | `080-3999-0379` | `tel:+818039990379` / `+81-80-3999-0379` |
| 固定電話（復活待ち） | `0476-29-5498` | `tel:+81476295498` / `+81-476-29-5498` |
| FAX（変更なし） | `0476-33-4744` | — |

> FAX は電話番号統一の対象外。そのまま残しています。

## 復活させたいとき（固定電話を再表示）

### 1. 復活ポイントを探す

各 HTML ファイルの復活ポイントには、すべて検索用マーカー **`固定電話復活`** を埋め込んであります。
プロジェクトルートで以下を実行すると、全21箇所が一覧で出ます。

```powershell
# PowerShell
Select-String -Path dev\site\*.html -Pattern "固定電話復活"
```

```bash
# Git Bash 等
grep -rn "固定電話復活" dev/site/*.html
```

### 2. コメントを解除する

復活ポイントは次の2パターンです。マーカー行の直後を見れば、どちらか判別できます。

**パターンA：固定電話の行をコメント解除するだけ**
（携帯はすでに別行で表示されているので、固定電話を「足す」だけ）

```html
<!-- 固定電話復活: 次行のコメント解除で固定電話を再表示 -->
<!-- <li>...固定電話...</li> -->   ← この行の <!-- と --> を外す
<li>...携帯...</li>
```

**パターンB：固定電話を戻し、携帯のみの行を消す**
（footer や 一覧表など、固定電話＋携帯を1行にまとめていた箇所）

```html
<!-- 固定電話復活: 下のコメント解除＋携帯のみの行を削除で元に戻る -->
<!-- <p>...固定電話 / 携帯...</p> -->   ← この行の <!-- と --> を外す（＝固定電話＋携帯の併記に戻る）
<p>...携帯のみ...</p>                    ← この行は削除する
```

> マーカー行（`<!-- 固定電話復活: ... -->`）自体は消しても残しても表示には影響しません。
> 残しておけば次回また切り替えるときに楽です。

### 3. JSON-LD（構造化データ）だけは手作業で戻す

`dev/site/index.html` の先頭付近にある JSON-LD は、JSON のためコメントが使えません。
値を直接書き換えてあります。固定電話に戻すときは次のように変更してください。

```jsonc
// 現在
"telephone": "+81-80-3999-0379",
// 固定電話に戻す場合
"telephone": "+81-476-29-5498",
```

## 復活ポイント一覧（全21箇所 ＋ JSON-LD）

| ファイル | 箇所 | パターン |
|---|---|---|
| index.html | 上部バー連絡先 / モバイルメニュー / ヒーロー帯 / 下部CTA「お電話」/ フッターTEL | A / A / B / B / B |
| index.html | JSON-LD `telephone` | 手作業（上記3参照） |
| about.html | 上部バー / モバイルメニュー / 会社情報ボタン / 会社情報テーブル「電話 / FAX」/ 下部CTA / フッターTEL | A / A / B / B / B / B |
| contact.html | 上部バー / TEL欄（縦並び）/ フッターTEL / フッター上部CTA「お電話」 | A / A / B / B |
| services.html | 上部バー / フッターTEL / フッター上部CTA「お電話」 | A / B / B |
| fleet.html | 上部バー / フッターTEL / フッター上部CTA「お電話」 | A / B / B |

## 注意

- `taxix-client-files/`（テンプレート素材）と `proposal/`（営業提案 PPT）にも電話番号があるが、
  本番サイト（`dev/site/`）ではないため**今回は変更していない**。必要なら別途指示のこと。
- contact.html の問い合わせフォームにある「電話番号」入力欄は**お客様が入力する項目**なので無関係（変更なし）。
